{
  "name": "r2-browser-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for browsing R2 storage bucket contents",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.44.0"
  },
  "dependencies": {
    "itty-router": "^4.0.0"
  }
}
